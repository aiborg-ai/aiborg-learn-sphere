# Enable lockfile v2 for better deduplication
lockfile-version=2

# Disable optional dependencies to reduce install size
optional=false

# Enable hoisting for smaller node_modules
hoist=true

# Use legacy peer deps to avoid conflicts
legacy-peer-deps=true
